import styled from 'styled-components';

const Base = styled.div`
    .grid-container {
        display: flex;
        flex-direction: row;
        justify-content: center;
    }
    h1 {
        margin: 0;
        padding: 10px 0;
    }
`;

export default Base;